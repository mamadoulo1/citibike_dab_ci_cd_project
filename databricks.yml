# This is a Databricks asset bundle definition for citibike_dab_ci_cd_project.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: citibike_dab_ci_cd_project
  uuid: de6e7578-ca40-4d77-a425-7538946e5eef

include:
  - resources/*.yml
  - resources/*/*.yml




# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    default: "citibike_dev"
    description: The catalog to use
  dev_flag:
    default: true

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-7405606406847903.3.azuredatabricks.net


  test:
    mode: production
    variables:
      catalog:
        default: "citibike_test"
      dev_flag:
        default: false  
    workspace:
      host: https://adb-7405619857403381.1.azuredatabricks.net/
      # We explicitly deploy to /Workspace/Users/lomamadou95@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Shared/.bundle/${bundle.name}/${bundle.target}
      
    permissions:
      - user_name: lomamadou95@gmail.com
        level: CAN_MANAGE


  prod:
    mode: production
    variables:
      catalog:
        default: "citibike_prod"
      dev_flag:
        default: false
    workspace:
      host: https://adb-7405615258649692.12.azuredatabricks.net/
      # We explicitly deploy to /Workspace/Users/lomamadou95@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Shared/.bundle/${bundle.name}/${bundle.target}

    permissions:
      - user_name: lomamadou95@gmail.com
        level: CAN_MANAGE

